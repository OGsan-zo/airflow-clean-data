# ETL & AIRFLOW

### Utilisation de AIRFLOW
Pour lancer airflow : 
    - Aller dans `docs\installation\lancement\`
    - Lancer Airflow avec `start_airflow.sh`
    - Arreter Airflow avec `stop_airflow.sh`